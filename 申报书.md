# 2026 MoonBit 国产基础软件开源大赛 项目申报书

## 1. 项目名称
**moonbit-toml** (MoonBit TOML Data Engineering Toolkit)

---

## 2. 项目简介
`moonbit-toml` 是一款基于 **MoonBit** 语言开发的**生产级 TOML 数据工程工具包**。

目前 MoonBit 生态在配置处理领域尚缺乏功能完善的底层基础设施。与社区现有的基础解析器（如 `bobzhang/toml`）不同，本项目**定位为应用级工具包**，专注于解决开发者在实际工程中的**高阶痛点**：如何合并配置、对比差异、跨格式迁移数据、动态构建复杂结构以及进行 Schema 校验。

本项目不仅提供标准的 TOML v1.0.0 解析功能，还创新性地引入了 **Builder 构建模式**、**配置合并/差异分析 (Merge/Diff)**、**多格式互操作 (JSON/XML/SQL/CSV)** 以及**结构统计 (Statistics)** 等企业级特性，旨在成为 MoonBit 生态中处理配置数据的**一站式解决方案**。

---

## 3. 项目方向与适用场景

### 项目方向
- **原创生态库**：MoonBit 基础工具链/配置管理库

### 适用场景
1.  **微服务与后端开发**：用于解析服务配置文件（如 `config.toml`），提供**配置热重载**与**多环境配置合并 (Merge)** 功能。
2.  **数据迁移与互操作**：在 MoonBit 生态中充当数据交换桥梁，实现 TOML 与 JSON、YAML、CSV、SQL DDL 之间的无缝映射，辅助旧系统迁移。
3.  **DevOps 与 CI/CD**：自动化脚本中的**配置差异分析 (Diff)**、**版本对比**、**模板生成**。
4.  **工具链开发**：作为构建系统或 CLI 工具的底层配置引擎，提供强类型访问与路径查询 (Query)。

---

## 4. 拟实现的核心功能

本项目已完整实现并交付以下核心功能模块：

| 模块分类 | 核心功能与 API | 状态 |
| :--- | :--- | :--- |
| **基础解析/序列化** | `parse`, `serialize`, `serialize_pretty`, `serialize_sorted` | ✅ 已实现 |
| **配置生命周期** | `merge`, `shallow_merge`, `diff`, `deep_equal` | ✅ **核心亮点** |
| **多格式互操作** | `to_json_like`, `to_xml_like`, `to_yaml_block`, `to_csv`, `to_sql_create` | ✅ **核心亮点** |
| **路径与查询** | `parse_path`, `resolve_path`, `find_by_wildcard`, `flatten` | ✅ 已实现 |
| **配置构建器** | `builder_table`, `builder_array`, `builder_set_*`, `builder_nested_table` | ✅ 已实现 |
| **校验与 Linter** | `validate`, `validate_schema`, `lint_toml`, `lint_mixed_types` | ✅ 已实现 |
| **统计与属性** | `compute_stats`, `count_nodes`, `hash_value`, `fingerprint` | ✅ 已实现 |

### 📊 技术指标达成情况
- **有效代码量**：**~5,500 行** (MoonBit)，远超预期标准。
- **编译质量**：**0 Errors** (零错误编译)，严格遵循 MoonBit 语法约束。
- **测试覆盖**：**19 项核心白盒测试** (`moon test` 100% 通过)。
- **CLI 演示**：内置 `moon run cmd/main` 演示工具，直观展示配置合并与差异分析。

---

## 5. 原创性说明

- **原创声明**：本项目为**原创生态库**。
- **创作模式**：由创作人 **郭亚斌** 主导架构设计与核心逻辑实现，辅以 **AI** 进行代码生成、语法校验、重构建议与测试脚本编写 (个人 + AI 创作)。
- **与现有项目的区别与互补性**：
  - **定位差异**：社区现有的 `bobzhang/toml` 是一个优秀的**基础解析器**，专注于 TOML 1.1 规范的严格合规。而本项目 (`moonbit-toml`) 定位为**面向工程应用的高阶工具包**，不追求替代基础解析器，而是填补生态中**配置生命周期管理**的空白。
  - **技术独立性**：项目**未移植**自任何现有的 C/Rust/Go 实现，而是从零开始基于 MoonBit 的**代数数据类型 (ADT)**、**模式匹配**及**内部可变性 (Interior Mutability)** 机制原生构建。
  - **开源协议**：采用 **Apache-2.0** 许可证，开源协议友好。

---

## 附录：项目信息

- **申报人**：郭亚斌
- **仓库地址**：https://github.com/santiagoguo/moonbit-toml
- **发布日期**：2026-05-29
